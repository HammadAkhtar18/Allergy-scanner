name: allergy_scanner
publish_to: 'none'
description: Barcode-based allergy scanner
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  mobile_scanner: ^5.2.3
  provider: ^6.1.2
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true
